{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{try{console.log(JSON.parse(d).tool_input.file_path||'')}catch(e){}})\"); case \"$f\" in *.dart) out=$(dart analyze --fatal-infos \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; } ;; esac",
            "timeout": 90,
            "statusMessage": "dart analyze"
          }
        ]
      }
    ]
  }
}
